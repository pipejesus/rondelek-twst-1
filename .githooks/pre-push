#!/usr/bin/env bash
#
# pre-push — runs before refs are pushed to the remote.
# Activate (once, per clone) with:
#   git config core.hooksPath .githooks
#
set -euo pipefail

echo "pre-push stub — no checks enabled yet."

# TODO (return later): gate pushes on lint + tests so red code never leaves the machine.
# cargo clippy --all-targets --all-features -- -D warnings
# cargo test --locked
